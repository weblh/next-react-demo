import { RouterButton } from "@/components/RouterButton";
export default function Login(){
    return(
        <main>
            <RouterButton />
            <div>login</div>
        </main>
    )
}